# Database (Railway PostgreSQL ou local)
DATABASE_URL="postgresql://revsend:revsend123@localhost:5432/revsend"

# Redis (Railway Redis ou local)
REDIS_URL="redis://localhost:6379"

# NextAuth
NEXTAUTH_SECRET="your-super-secret-key-change-this-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Anthropic Claude API
ANTHROPIC_API_KEY="sk-ant-..."

# Encryption (32 caracteres para AES-256)
ENCRYPTION_KEY="change-this-to-32-byte-secret-key!"

# Sentry (Error Tracking)
NEXT_PUBLIC_SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"
SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"
SENTRY_ORG="your-org"
SENTRY_PROJECT="revsend"
SENTRY_AUTH_TOKEN="sntrys_your_auth_token"

# App URL (usar URL do Railway em produção)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node Environment
NODE_ENV="development"

# Storage (Railway Volume mount point)
# Local: ./storage (default)
# Railway: /app/storage (Railway Volume)
STORAGE_DIR="/app/storage"
